# TR AI Platform workspace ID — used to exchange for a short-lived Anthropic API key
WORKSPACE_ID=your_workspace_id_here

# Model to use (latest sonnet recommended)
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# Optional: set directly if not using TR AI Platform token exchange
# ANTHROPIC_API_KEY=sk-ant-...
